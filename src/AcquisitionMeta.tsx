export const originNames:Record<string,string>={automatic_subscription:'自动订阅',manual_subscription_check:'手动检查订阅',manual_catalog:'目录采集',manual_url:'手动链接',manual_import:'手动导入'};
export const providerNames:Record<string,string>={weread:'微信读书 · 免费',cimidata:'次幂 · 付费',browser:'公开页面 · 免费',public:'公开页面 · 免费',local:'本地文稿'};
export const fullTime=(value?:string)=>value?new Date(value).toLocaleString('zh-CN',{hour12:false}):'未记录';
export function AcquisitionMeta({item}:{item:any}){return <div className="acquisition-meta"><span className="origin-label">{originNames[item.acquisition_origin||item.discovery_origin]||'历史记录 · 来源未细分'}</span><span>{item.acquisition_provider?'正文：':item.provider?'目录：':''}{providerNames[item.acquisition_provider||item.provider]||item.source_type||'来源未记录'}</span><span>发布：{item.published||'未知'}</span><details><summary>获取时间</summary><dl>{item.discovery_origin&&<><dt>目录来源</dt><dd>{originNames[item.discovery_origin]||'未记录'}</dd></>}<dt>首次发现</dt><dd>{fullTime(item.discovered_at)}</dd><dt>正文保存</dt><dd>{fullTime(item.body_saved_at)}</dd>{item.last_attempt_at&&<><dt>最近尝试</dt><dd>{fullTime(item.last_attempt_at)}</dd></>}</dl></details></div>}
